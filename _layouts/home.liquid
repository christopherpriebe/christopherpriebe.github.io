---
layout: base
---

<div class="container">
    <div id="bio" class="row">
        <div class="col-xs-12 text-center">
            <div class="row">
                <div class="col-xs-12 text-center-xs col-md-6 text-right-md">
                    <img src="/assets/{{ site.profile.image }}" alt="Headshot of Christopher Priebe" class="bio-pic img-circle">
                </div>
                <div class="col-xs-12 text-center-cs col-md-6 text-left-md">
                    <div class="h1">{{ site.profile.first_name }} {{ site.profile.middle_name }} {{ site.profile.last_name }}</div>
                    <div class="h4">{{ site.profile.title }}</div>
                    <div class="h5">{{ site.profile.affiliation }}, {{ site.profile.location }}</div>

                    {% if site.github_username %}
                        <a class="bio-social-icon-link" href="https://github.com/{{ site.github_username }}"><i class="fa fa-github bio-social-icon"></i></a>
                    {% endif %}
                    {%  if site.linkedin_username %}
                        <a class="bio-social-icon-link" href="https://linkedin.com/in/{{ site.linkedin_username }}"><i class="fa fa-linkedin-square bio-social-icon"></i></a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12">
            <hr style="margin-top: 20px; margin-bottom: 20px;">
        </div>
    </div>
    <div id="history" class="row row-md-height-columns">
        <div class="col-xs-12 text-center">
            {% for item in site.profile.history %}
                <div class="row row-no-gutters history-item">
                    <div class="col-xs-3 col-md-1 text-right history-item-span">
                        <span>{{ item.start_year }} - {{ item.end_year }}</span>
                    </div>
                    <div class="col-xs-9 col-md-2 text-center history-item-logo">
                        <div class="history-item-dot"></div>
                        <img src="/assets/{{ item.logo }}" class="img-rounded img-responsive" alt="Logo for experience" class="company-logo">
                    </div>
                    <div class="col-xs-8 col-xs-offset-3 col-md-9 col-md-offset-0 text-left history-item-content">
                        <p>{{ item.description }}</p>
                    </div>
                </div>
            {% endfor %}
        </div> 
    </div>
</div>
