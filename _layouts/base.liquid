<!DOCTYPE html>
<html lang="{{ page.lang | default: site.lang | default: "en-US" }}">

    {%- include head.liquid -%}

    <body>

        {%- include header.liquid -%}

        <main class="page-content" aria-label="Content">
            {{ content }}
        </main>

        {%- include footer.liquid -%}

        {% include scripts/bundle.liquid %}
    </body>

</html>
